# rc.conf - hello_initsys configuration
#
# shell: program to spawn as PID 1's child (looked up by name in the rootfs).
# respawn: whether to spawn a fresh copy each time it exits.

shell = "sh"
respawn = true
